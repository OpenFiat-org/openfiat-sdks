# Reference data

Canonical reference data for the OpenFiat ecosystem, consumed by SDKs and
applications: countries, currencies, banks, payment methods, and languages.

Data files are plain JSON so they can be consumed from any language without
depending on this repository's Python tooling. `validate.py` checks schema
conformance and is run in CI.
