# Architecture — openfiat-sdks

> Diagram source lives alongside this file (Mermaid preferred so it renders on GitHub).

```mermaid
flowchart LR
    A[openfiat-sdks] --> B[OpenFiat Network]
    B --> C[Solana Settlement Layer]
```

Describe major components, data flow, and external dependencies here as the implementation lands.
